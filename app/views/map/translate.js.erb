/* jshint ignore:start */
/*
a partial for translating a string and sending it to a callback function
example ajax call
$.ajax({
  url: 'map/translate', 
  data: {
    string: 'redesignated', // <- this is the JS variable string that will be used for the localization
    translate_prefix: 'legend.', // <- accepts empty as well
    callback: 'HZApp.Legend.insertLegendItemTitle', // <- string that points to the callback method that will use the values
    locale: 'en', // <- string of the current page locale
  },
  success: , // <- may not be needed, since the successful return of this partial will run itself
  error: function(){} // <- do something in event of a failure in running the partial
  
})

this will return a call to the callback function with the first arg the localized string, and the second the full hash:
HZApp.Legend.insertLegendItemTitle("Redesignated", JSON.parse('{"string":"redesignated","translate_prefix":"legend.","callback":"HZApp.Legend.insertLegendItemTitle","locale":"en","controller":"map","action":"translate"}'));
*/
<%=@body[:callback] %>("<%= t(@body[:translate_prefix]+@body[:string]) %>", JSON.parse('<%= @body.to_json.html_safe %>')); 
/* jshint ignore:end */
